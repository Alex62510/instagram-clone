import React from 'react';
import Feed from "@/components/Feed";

const Home = () => {
    return (
        <div className={'text-red-500'}>
          <Feed/>
        </div>
    );
};

export default Home;